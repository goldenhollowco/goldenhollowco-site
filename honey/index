<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>The Honey Hollow ✧ — Age Gate</title>
  <link href="https://fonts.googleapis.com/css2?family=Gloock&family=Quicksand:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      background-color: #0a0809;
      color: #f8f0e6;
      font-family: 'Quicksand', sans-serif; 
      margin: 0;
      padding: 2rem; 
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      text-align: center;
    }
    .card {
      background: #1c1410;
      border: 1px solid #805c3e;
      padding: 2rem;
      border-radius: 14px;
      max-width: 520px;
      box-shadow: 0 12px 30px rgba(0,0,0,.5);
    }
    h1 {
      font-family: 'Gloock', serif;
      font-size: 2.2rem;
      color: #d4aa7d;
      margin-top: 0;
    }
    p {
      color: #e1c7ae;
      line-height: 1.5;
    }
    button {
      background: linear-gradient(180deg,#d4aa7d,#a97722);
      color: #111;
      font-weight: bold;
      border: none;
      padding: 0.9rem 1.5rem;
      border-radius: 999px;
      cursor: pointer;
      margin-top: 1.5rem;
      font-family: 'Quicksand', sans-serif;
      transition: filter 0.2s ease, transform 0.1s ease;
    }
    button:hover { filter: brightness(1.05); transform: translateY(-2px); }
    .note { font-size: 0.85rem; margin-top: 1rem; color: #b99c72; }
    a { color: #d4aa7d; text-decoration: none; }
  </style>
  <script>
    // If already confirmed, skip the gate
    if (localStorage.getItem('hh_isAdult') === 'true') {
      window.location.replace('links.html');
    }
  </script>
</head>
<body>
  <div class="card">
    <h1>Honey Hollow ✧</h1>
    <p>Whispers behind this gate are 18+ only. Step gently, and only if you're meant to.</p>
    <button onclick="confirmAge()">✧ Enter Honey Hollow (18+)</button>
    <p class="note">or <a href="../index4.html">go back</a></p>
  </div>

  <script>
    function confirmAge(){
      localStorage.setItem('hh_isAdult','true');
      window.location.href='links.html';
    }
  </script>
</body>
</html>
