<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Honey Hollow ✧ — Age Gate</title>

  <!-- Fonts to match your brand -->
  <link href="https://fonts.googleapis.com/css2?family=Gloock&family=Quicksand:wght@400;600&display=swap" rel="stylesheet">

  <!-- If they already confirmed on this device, skip the gate -->
  <script> 
    if (localStorage.getItem('hh_isAdult') === 'true') {
      window.location.replace('links.html');
    }
  </script>

  <style>
    /* ===== Dark Honey theme (age gate) ===== */
    :root{
      --bg:#0a0809;        /* espresso */
      --card:#1c1410;      /* panel */
      --ink:#f8f0e6;       /* warm ivory text */
      --muted:#e1c7ae;     /* soft accent text */
      --ring:#805c3e;      /* outline */
      --honey:#d4aa7d;     /* gold */
      --honey-2:#a97722;   /* deeper gold */
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      background: var(--bg);
      color:var(--ink);
      font-family:'Quicksand',sans-serif;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      padding:2rem;
    }

    .card{
      background: var(--card);
      border:1px solid var(--ring);
      border-radius:14px;
      width:min(520px,100%);
      padding:2rem;
      box-shadow:0 12px 30px rgba(0,0,0,.5);
    }

    h1{
      font-family:'Gloock',serif;
      font-size:2.2rem;
      margin:0 0 .5rem 0;
      color:var(--honey);
      letter-spacing:.2px;
    }

    p.lede{
      margin:.25rem 0 1.25rem 0;
      color:var(--muted);
      line-height:1.55;
    }

    .btn{
      appearance:none;
      border:none;
      border-radius:999px;
      padding:.9rem 1.5rem;
      font-weight:700;
      font-family:'Quicksand',sans-serif;
      cursor:pointer;
      background:linear-gradient(180deg,var(--honey),var(--honey-2));
      color:#111;
      box-shadow:0 10px 26px rgba(0,0,0,.45);
      transition:transform .12s ease, filter .2s ease;
    }
    .btn:hover{ transform:translateY(-2px); filter:brightness(1.05); }
    .btn:active{ transform:translateY(0); filter:brightness(.98); }

    .note{
      margin-top:1rem;
      font-size:.9rem;
      color:#c8a98e;
    }
    .note a{
      color:var(--honey);
      text-decoration:none;
      border-bottom:1px dotted var(--honey);
      padding-bottom:2px;
    }
    .note a:hover{ filter:brightness(1.05); }
  </style>
</head>
<body>
  <main class="card">
    <h1>Honey Hollow ✧</h1>
    <p class="lede">
      Whispers behind this gate are 18+ only. Step gently, and only if you're meant to.
    </p>

    <button class="btn" onclick="confirmAge()">
      ✧ Enter Honey Hollow (18+)
    </button>

    <p class="note">or <a href="../index.html">go back</a></p>
  </main>

  <script>
    function confirmAge(){
      localStorage.setItem('hh_isAdult','true');
      window.location.href = 'links.html';
    }
  </script>
</body>
</html>
